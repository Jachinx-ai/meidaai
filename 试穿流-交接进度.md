# 试穿流（前端B）· 换机交接文档

> 给接手本项目的 **Claude（无历史记忆）**：这份文档是你的全部上下文。读完它 + 根目录 `CLAUDE.md`，就能直接继续工作。
> 最后更新：2026-07-08。

---

## 0. 一句话现状

我负责 **前端B·试穿流**（`tryon.html` / `create-model.html` / `history.html`）。
最近做了一件大事：**把虚拟试穿引擎从阿里 aitryon-plus 换成了 gemini-3.1 生图模型**（鞋子终于能生成、模特不换脸），并把生成等待期改成了**加载态**（不再穿帮）。
目前在 `frontend-b-tryon-flow` 分支上，有 6 个提交，外加**一批未提交的 bodymask 改动**（待团队决策，见第 3 节，别弄丢）。

---

## 1. 我是谁 / 我的地盘 / 红线

- 身份：8 人学生团队里的 **前端B·试穿流**。
- **只改这三个页面**：`tryon.html`（模特试穿页，全项目最复杂）、`create-model.html`（创建模特页）、`history.html`（试衣间页）。
- **例外**：因为换引擎，我这次也改了 `server/ai/config.js` 和 `server/ai/tryon.js`（AI 能力层，本属"AI效果"同学李超的地盘）。这是产品负责人授权的，可以继续改。
- **公共四件套别碰**：`css/base.css`、`js/app.js`、`js/data.js`、`js/ai.js` —— 全队只有刘倩能改。如果发现 bug 需要改这些，**不要直接改**，写清楚需求让用户去找刘倩。
- 其它红线见 `CLAUDE.md`：纯 HTML+CSS+原生JS、禁框架/打包工具、`js/data.js` 脚本生成禁手改、图片一律 `compressImage()` 压缩再存、防烧钱机制勿删。

---

## 2. 怎么跑起来 + 密钥

```bash
cd server && npm install && npm start
# → http://localhost:8394/login.html
```

- **密钥已配好**：`server/.env` 里有真实的 `OPENROUTER_API_KEY`（现在试穿/搭配/识别都靠它）和 `DASHSCOPE_API_KEY`（换引擎后已不用，留着无害）。这个文件在 `.gitignore` 里、不进 git，但会随文件夹打包过来，所以新机器开箱即用。**别把密钥贴进任何会提交/外发的文件。**
- **⚠️ 花钱提醒**：现在每次换装/打开试穿页都会真实调用 gemini-3.1 生成，**约 ¥0.5/张、约 10~30 秒**。调试时注意别狂点。想零成本纯改 UI，就把 `.env` 里的 `OPENROUTER_API_KEY` 清空（会自动降级为本地叠图）。
- 重置演示数据：任意页面 URL 加 `?reset=1`。

---

## 3. ⚠️ 当前 git 状态（最重要，别弄丢工作区改动）

**分支**：`frontend-b-tryon-flow`（只在本地，**没有推远程**，也别推——用户要求本地开发）。

**已提交的 6 个（`git log main..HEAD`）**：
| commit | 内容 | 状态 |
|---|---|---|
| `d0aee75` | 生成等待期改为加载态（见第 3 节说明）| ✅ |
| `faf7606` | 试穿引擎换 gemini-3.1（见第 4 节） | ✅ |
| `8186cb3` | 换鞋不重复计费（缓存key剔除鞋子）| ⚠️ **已被 faf7606 覆盖作废**（换引擎后鞋子真参与生成了，缓存key又纳回鞋子） |
| `91580bc` | 试衣间删空不再自动重生 | ✅ |
| `c76482b` | 按钮连点防重复计费（临时方案）| ✅（最终交互待定，见 6节讨论项②）|
| `b727d54` | 试穿页锁定一屏、模特不被滑走 | ✅ |

**加载态改动已提交（`d0aee75`）**：gemini 生成的十几秒等待期不再显示会"穿帮"的临时叠图，改为**加载态**（模特图 + 模糊蒙层 `.gen-loading` + 转圈 + "生成中"提示）。已浏览器实测通过。

**bodymask 已决策废弃（2026-07-08，产品负责人拍板，工作区现已完全干净）**：
原为"叠图穿帮"的缓解方案（白雾片盖默认模特原装衣服），一直未提交。决策结果：**线上生成失败不再回退叠图**——
台上保持原样（旧生成图或纯模特）+ toast"生成失败，请重试"（commit `a8cc3a7`）；平铺叠图仅保留给无密钥/后端
不在线的演示模式（后端返回 `mock:true` 标记预期降级，与真失败区分），bodymask 因此失去服务对象、整体废弃。
工作区已无任何未提交改动，之后正常 `git add` 即可，不再需要选择性提交。

---

## 4. 核心架构变更：试穿引擎换成 gemini-3.1（已提交 faf7606）

**为什么换**：旧的 aitryon-plus（阿里百炼 VTON 模型）有两个硬伤——① 输入格式写死只支持"人+上装+下装"，**鞋子根本塞不进去**；② 而且它在旧代码里因为上传缺 MIME 类型，其实 100% 失败在回退叠图。

**做了什么对比测试**（结论，供你判断，不用重测）：
- gemini-3.1-flash-image（Nano Banana 2）：一步喂"模特+上衣+下装+鞋子"→ 生成整套上身图，**鞋子能生成、且保住模特本人的脸**。~¥0.5/张。✅ 选它。
- gemini-2.5-flash-image：更便宜(~¥0.28)，但**会把模特换成另一个人**（换脸），不能用。
- 阿里 shoemodel-v1（专门试鞋）：换鞋好，但整图重绘也会**轻微换脸**；且不在 OpenRouter。
- 全套换其它 VTON 模型：市面没有"一步连鞋一起生成"的成熟 API，放弃。

**怎么改的**：
- `server/ai/config.js`：`MODELS.tryon` 从 `"aitryon-plus"` 改成 `"google/gemini-3.1-flash-image"`。
- `server/ai/tryon.js`：整个重写。走 OpenRouter，把 模特图+上衣/连体裙+下装+鞋子 组成多图消息，一步生成。契约不变（输入 `{modelImage, items}` → 输出 `{image|null}`，null 时前端叠图兜底）。
- `tryon.html` 三处配套（已在 faf7606）：genImage 分支不再前端叠鞋（鞋子已在生成图里）、缓存 key 重新纳入鞋子、快照直接用生成图（`compressImage`）。

**连带影响 / 已知副作用**：
- 默认模特（`assets/real/model-f.png`）现在也能真生成了（旧模型要求真人照片）。
- gemini 是"理解后重画"：纯色/基础款还原好；**复杂印花/logo/文字可能会糊或变形**；每次结果有细微随机差异。这是生图模型的固有边界，不是 bug。
- 提示词在 `server/ai/tryon.js` 里内联（不在 prompts.js）。要调效果就改那段中文 prompt。

---

## 5. 关键机制 / 前人踩过的坑（改代码前先懂这些）

- **防烧钱三件套（勿删）**：换装后 600ms 防抖（`applyTryon`）；同模特+同一身衣服结果内存缓存（`genCache`）；过期结果按序号 `tryonSeq` 丢弃。加载态改动已把"命中缓存"做成秒显不重复计费。
- **加载态状态机**（改完后）：`renderStage()` 优先级 = 有生成图 `genImage` > 生成中 `generating`（加载态）> 兜底叠图。
- **鞋子**：模型已能生成鞋子，前端**不要再叠鞋**（faf7606 已删叠鞋逻辑）。
- **舞台底色**：`syncStageBg()` 自动取当前图片边缘色，别写死颜色。
- **localStorage 5MB 上限**：所有进 `Store` 的图必须先 `compressImage`。换 gemini 后每张快照都是生成大图，**超限风险更高了**（这是待修问题，见下）。
- **后端"假离线"**：前端探测后端只等 1.5s，超时静默降级本地叠图。"AI 没生效"先确认 `npm start` 还活着。

---

## 6. 待解决问题清单（带解决思路）

### 💬 待团队/产品决策（不写代码，等结论）
1. ✅ **bodymask 去留**：已决策废弃（2026-07-08，见第 3 节，commit `a8cc3a7`）。失败=保持原画面+toast重试；叠图仅留给离线/无密钥演示模式。
2. ✅ **防连点最终交互**：已决策（2026-07-08 产品负责人）——**维持现状**，不做独立生成页。重复计费已由按钮锁定+600ms防抖+结果缓存+失败10秒冷静期四层封死。
3. ✅ **创建模特两条路径不一致**：已决策并实施（commit `9f6385c`）——**统一为停留确认**：预设点卡片只高亮、上传成功只入库作候选展示，都要点「确认」才生效并返回。
4. **上传提示的"错误示范"图**：`create-model.html` 里坏例子全是同一张剪影乱变形，语义不达意，需设计出真实示例图。

### 🔴 待修 · 高（三项已全部处理，2026-07-08）
1. ✅ **切模特后返回试穿页不刷新**：已修（commit `a093009`）。tryon.html 加 `pageshow` 监听：bfcache 恢复时重读 localStorage，模特换过 → 作废旧生成图、显示新模特并进加载态重新生成（不闪叠图）；没换 → 命中缓存秒显不计费。真实 gemini 出图验证通过。
2. ✅ **localStorage 超限无保护**：已修（commit `bedfb6d`），**刘倩已审阅确认为正式方案（2026-07-08）**。方案：`Store.set` 写入失败时从最老的试衣间快照逐条丢弃腾空间重试（toast 告知），全丢仍不够则 toast"存储已满"、内存数据保留页面继续可用。三条路径均实测通过。
3. ✅ **create-model 自动返回无防重入**：已修（commit `30d8b3c`）。确认按钮/预设卡片点击即上锁（按钮变灰"正在返回…"，连点只预约一次返回）；无上一页可退时兜底跳转试穿页（产品负责人定的落点）。

### 🟡 待修 · 中

> 2026-07-08 剩余 5 项（6/8/9/10/12）已向产品负责人当面讲解过影响与修法，建议顺序 10→9→8→6→12
> （前三个小改动，6 需顺手拍产品细节，12 需先定布局样式）。**决定暂缓修改**，先做试穿流整体链路审查。
4. ✅ 生成失败的组合反复重试反复计费——已修（commit `145e004`）：失败组合 10 秒冷静期（产品负责人定），期间重试不发请求、toast"刚失败过请稍等几秒再试"；冷静期只存页面内存。
5. ✅ 删除当前模特照片显示错乱——已修（commit `fba080c`）：删当前生效照片自动顺位切到剩余第一张（显示=生效），删光才回默认模特；删非当前照片生效不动。
6. `?outfit=` 旧链接绕过性别/删除过滤，还能产生"永远看不见的幽灵收藏"；无参进入固定穿女装第一套。
7. ✅ `wearing` 为空也发生成请求并闪"约需20秒"——已修（commit `a8cc3a7`）：applyTryon 空衣服直接跳过、显示纯模特。
8. `?item=` 从衣橱进来绕过"须先上传"检查。
9. 图片压缩失败时原图直存（放大容量风险、坏图能建成模特）。
10. 删除模特照片无确认弹窗（删衣服有，全站不一致）。
11. ✅ 试衣间查看器切缩略图卡顿——已修（commit `f62788b`）：缩略图条打开时建一次，切换只换主图+挪 `.on` 选中框，不再全量重建。
12. create-model 多照片管理缺口（只能看/删当前一张）。

### 🟢 待修 · 低（攒一波清）
名称未转义拼进 alt、`syncStageBg` 竞态、穿搭 tab 空态、~~本地兜底搭配不分性别~~（✅ 已修 commit `b39e8a5`，刘倩本人授权并确认口径：按服饰性别偏好过滤系统款、上传衣服不限）、全量重渲染性能、横屏适配、空列表可进编辑态、`FileReader` 无 `onerror`、老安卓 EXIF 方向。

---

## 7. 建议的下一步

1. 按 6 节"高"优先级推进：问题1（bfcache 刷新）、问题3（防重入）——都在页内可修；问题2（存储超限）转刘倩。（改 `tryon.html` 时注意选择性提交，别把工作区那批 bodymask 带进去。）
3. 每完成一个改动主题做一次 git commit（中文说明改了什么、怎么验证的）。
4. UI 改动一律在 375px 手机宽度下、用真实 gemini 出图验证过再交付。

---

## 8. 交接确认清单（新 Claude 开工前自检）

- [ ] `cd server && npm install && npm start` 能起来，`localhost:8394/login.html` 打得开
- [ ] `git branch` 在 `frontend-b-tryon-flow`；`git status` 看到 `tryon.html` 有未提交改动（就是那批 bodymask）
- [ ] `server/.env` 存在且有 `OPENROUTER_API_KEY`
- [ ] 读过根目录 `CLAUDE.md`
- [ ] 打开试穿页换一套衣服，能看到"加载态 → gemini 生成图"，脚上鞋不重复
